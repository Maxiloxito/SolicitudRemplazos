<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= @job_offer.title %></title>
</head>
<body>
    <h1><%= @job_offer.title %></h1>

    <p><strong>Cargo:</strong> <%= @job_offer.position %></p>
    <p><strong>Descripción:</strong> <%= @job_offer.description %></p>
    <p><strong>Salario:</strong> $<%= @job_offer.salary %></p>
    <p><strong>Disponibilidad:</strong> <%= @job_offer.availability %></p>

    <%= link_to "Editar", edit_job_offer_path(@job_offer) %> |
    <%= link_to "Volver", job_offers_path %>
</body>
</html>
