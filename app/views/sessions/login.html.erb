<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <%= stylesheet_link_tag 'logincss', media: 'all' %>
</head>
<body>

    <div class="login-container">
        <h1 class="login-title">Login</h1>
        <% if flash[:danger] %>
            <div class="error-message">
                <%= flash[:danger] %>
            </div>
        <% end %>

        <%= form_with(url: login_path, local: true, method: :post) do |f| %>
            <div class="form-group">
                <%= f.label :email, "Usuario" %>
                <%= f.text_field :email, class: 'login-input', placeholder: "Usuario" %>
            </div>
            <div class="form-group">
                <%= f.label :password, "Contraseña" %>
                <%= f.password_field :password, class: 'login-input', placeholder: "Contraseña" %>
            </div>
            <%= f.submit "Iniciar", class: 'login-button' %>
        <% end %>
        
        <p style="text-align: center;">¿No tienes cuenta? <%= link_to "Regístrate aquí", register_path %></p>
    </div>

</body>
</html>
